function receba_array_exiba_console(p1) {
    for (var i = 0; i < p1.length; i++) {
        var element = p1[i]

        console.log(element)
        
    }
}

var variavel = [
    123,
    'asldfkajs',
    true,
    false,
    null,
    'alison',
    123
]

receba_array_exiba_console(variavel)